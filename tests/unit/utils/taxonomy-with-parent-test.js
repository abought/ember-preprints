import taxonomyWithParent from 'preprint-service/utils/taxonomy-with-parent';
import { module, test } from 'qunit';

module('Unit | Utility | taxonomy with parent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = taxonomyWithParent();
  assert.ok(result);
});
