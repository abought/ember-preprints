{{#osf-navbar as |loginButton|}}
    <li class="dropdown sign-in">
        <div class="col-sm-12">
            <a href="{{this.authUrl}}" class="btn btn-success btn-top-signup m-r-xs">Login</a>
        </div>
    </li>
{{/osf-navbar}}


<div class="row">
    <div class="col-md-12">{{outlet}}</div>
</div>

{{osf-footer}}
<div class="text-center">{{osf-copyright}}</div>
{{osf-mode-footer}}